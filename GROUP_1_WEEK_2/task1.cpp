#include <iostream>
using namespace std;

int main()
{

  // -------- Task 0 --------

  // int num;
  // cin >> num;

  // cout << (num % 2 == 0 ? "YES" : "NO");

  // -------- Task 1 --------

  // char symbol;

  // cout << "Please enter an operation from the following (+, -, *, /): " << endl;
  // cin >> symbol;

  // double num1, num2;

  // cout << "Please enter two numbers with a floating point: " << endl;
  // cin >> num1 >> num2;

  // switch (symbol)
  // {
  // case '+':
  //     cout << double(num1) + double(num2);
  //     break;
  // case '-':
  //     cout << double(num1) - double(num2);
  //     break;
  // case '*':
  //     cout << double(num1) * double(num2);
  //     break;
  // case '/':
  //     cout << double(num1) / double(num2);
  //     break;
  // default:
  //     cout << "Enter a valid operation" << endl;
  // }

  // -------- Task 2 --------

  // double weight, height;

  // cout << "Enter your weight and height: ";
  // cin >> weight >> height;

  // double formula = (weight / (height * height)) * 10000;

  // cout << "Your body weight index is: " << formula << " Normal index (18.5-24.9)" << endl;

  // -------- Task 3 --------

  // int num;
  // cout << "Enter 4-digit number: ";
  // cin >> num;

  // int firstDigit = num / 1000;
  // int secongDigit = (num / 100) % 10;
  // int thirdDigit = (num % 100) / 10;
  // int fourthDigit = num % 10;

  // cout << "First digit: " << firstDigit << endl;
  // cout << "Second digit: " << secongDigit << endl;
  // cout << "Third digit: " << thirdDigit << endl;
  // cout << "Fourth digit: " << fourthDigit << endl;

  // if (firstDigit - secongDigit == 0 || firstDigit - thirdDigit == 0 || firstDigit - fourthDigit == 0)
  // {
  //     cout << "NO, not all numbers are different!";
  // }
  // else if (secongDigit - firstDigit == 0 || secongDigit - thirdDigit == 0 || secongDigit - fourthDigit == 0)
  // {
  //     cout << "NO, not all numbers are different!";
  // }
  // else if (thirdDigit - firstDigit == 0 || thirdDigit - secongDigit == 0 || thirdDigit - fourthDigit == 0)
  // {
  //     cout << "NO, not all numbers are different!";
  // }
  // else if (fourthDigit - firstDigit == 0 || fourthDigit - secongDigit == 0 || fourthDigit - thirdDigit == 0)
  // {
  //     cout << "NO, not all numbers are different!";
  // }
  // else
  // {
  //     cout << "YES, all the numbers are different!";
  // }

  // -------- Task 4 --------

  // int num;
  // cout << "Enter a number: ";
  // cin >> num;

  // if (num % 2 == 0)
  //     cout << "Yes";
  // else
  //     cout << "No";

  // -------- Task 5 --------

  // int a, b, c;

  // cout << "Enter sides for a triangle: ";
  // cin >> a >> b >> c;

  // bool check1 = a < b + c;
  // bool check2 = b < a + c;
  // bool check3 = c < a + b;

  // if (check1 && check2 && check3)
  //     cout << "YES, you can make a triangle out of this :)";
  // else
  //     cout << "NO, you can't make a triangle out of this :(";

  // -------- Task 6 --------

  // int x, y;
  // cout << "Enter values of x and y: ";
  // cin >> x >> y;

  // if (x > 0 && y > 0)
  //     cout << "First";
  // else if (x < 0 && y > 0)
  //     cout << "Second";
  // else if (x < 0 && y < 0)
  //     cout << "Third";
  // else if (x > 0 && y < 0)
  //     cout << "Fourth";
  // else
  //     cout << "On axis";

  // -------- Task 7 --------

  return 0;
}
